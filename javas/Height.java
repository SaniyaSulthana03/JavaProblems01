public class Height {
    if(exit condition) return *****;
    else{
    lh=height(root.left);
    rh=height(root.right);
    return max(lh,rh);  
    }
}
//find the node that has minimum data
//find the node that has maximum data
//if root node is to be deleted,which node can replace it without violating the BST property
//